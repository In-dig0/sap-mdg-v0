environments:
  default:
    connections:
      postgres:
        - name: "mdg_postgres"
          username: "${POSTGRES_USER}"
          password: "${POSTGRES_PASSWORD}"
          host: "postgres"
          port: 5432
          database: "${POSTGRES_DB}"
          ssl_mode: "disable"
          schema: "raw"